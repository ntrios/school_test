defmodule SchoolTest.Evaluation do
  @moduledoc """
  The Evaluation context.
  """
  import Ecto.Query, warn: false
  alias SchoolTest.Repo

  def create_evaluation do
    # Teacher create an activity model
  end

  def update_evaluation do
    # Teacher update an activity model
  end

  def apply_evaluation() do
    # Teacher applies an activity
  end

  def share_evaluation() do
    # Teacher share an activity with another teacher
  end

  def answer_evaluation() do
    # Student finishes an activity
  end

  def corrects_question() do
    # Teacher corrects individual question
  end

  def corrects_evaluation() do
    # Teacher corrects an activity
  end
end
